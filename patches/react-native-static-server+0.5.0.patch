diff --git a/node_modules/react-native-static-server/android/build.gradle b/node_modules/react-native-static-server/android/build.gradle
index 0c7bca6..1455754 100644
--- a/node_modules/react-native-static-server/android/build.gradle
+++ b/node_modules/react-native-static-server/android/build.gradle
@@ -4,43 +4,40 @@
 //
 // * https://github.com/facebook/react-native/blob/0.60-stable/template/android/build.gradle
 //   original location:
-//   - https://github.com/facebook/react-native/blob/0.58-stable/local-cli/templates/HelloWorld/android/build.gradle
+//   - https://github.com/facebook/react-native/blob/0.58-stable/local-cli/templates/HelloWorld/android/bu…
 //
 // * https://github.com/facebook/react-native/blob/0.60-stable/template/android/app/build.gradle
 //   original location:
-//   - https://github.com/facebook/react-native/blob/0.58-stable/local-cli/templates/HelloWorld/android/app/build.gradle
+//   - https://github.com/facebook/react-native/blob/0.58-stable/local-cli/templates/HelloWorld/android/ap…
  
-def DEFAULT_COMPILE_SDK_VERSION = 28
-def DEFAULT_BUILD_TOOLS_VERSION = '28.0.3'
-def DEFAULT_MIN_SDK_VERSION = 16
-def DEFAULT_TARGET_SDK_VERSION = 28
+def DEFAULT_COMPILE_SDK_VERSION = 35
+def DEFAULT_BUILD_TOOLS_VERSION = '35.0.0'
+def DEFAULT_MIN_SDK_VERSION = 24
+def DEFAULT_TARGET_SDK_VERSION = 35
  
 def safeExtGet(prop, fallback) {
     rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
 }
  
 apply plugin: 'com.android.library'
-apply plugin: 'maven'
+apply plugin: 'maven-publish'
  
 buildscript {
     // The Android Gradle plugin is only required when opening the android folder stand-alone.
     // This avoids unnecessary downloads and potential conflicts when the library is included as a
     // module dependency in an application project.
-    // ref: https://docs.gradle.org/current/userguide/tutorial_using_tasks.html#sec:build_script_external_dependencies
+    // ref: https://docs.gradle.org/current/userguide/tutorial_using_tasks.html#sec:build_script_external_depen…
     if (project == rootProject) {
         repositories {
             google()
-            jcenter()
+            mavenCentral()
         }
         dependencies {
-            classpath 'com.android.tools.build:gradle:3.4.1'
+            classpath 'com.android.tools.build:gradle:7.0.0'
         }
     }
 }
  
-apply plugin: 'com.android.library'
-apply plugin: 'maven'
-
 android {
     compileSdkVersion safeExtGet('compileSdkVersion', DEFAULT_COMPILE_SDK_VERSION)
     buildToolsVersion safeExtGet('buildToolsVersion', DEFAULT_BUILD_TOOLS_VERSION)
@@ -67,7 +64,7 @@ repositories {
         url "$rootDir/../node_modules/jsc-android/dist"
     }
     google()
-    jcenter()
+    mavenCentral()
 }
  
 dependencies {
@@ -80,29 +77,11 @@ dependencies {
 def configureReactNativePom(def pom) {
     def packageJson = new groovy.json.JsonSlurper().parseText(file('../package.json').text)
  
-    pom.project {
-        name packageJson.title
-        artifactId packageJson.name
-        version = packageJson.version
-        group = "com.futurepress.staticserver"
-        description packageJson.description
-        url packageJson.repository.baseUrl
-
-        licenses {
-            license {
-                name packageJson.license
-                url packageJson.repository.baseUrl + '/blob/master/' + packageJson.licenseFilename
-                distribution 'repo'
-            }
-        }
-
-        developers {
-            developer {
-                id packageJson.author.username
-                name packageJson.author.name
-            }
-        }
-    }
+    pom.name = packageJson.title
+    pom.artifactId = packageJson.name
+    pom.version = packageJson.version
+    pom.group = "com.futurepress.staticserver"
+    pom.description = packageJson.description
 }
  
 afterEvaluate { project ->
@@ -111,17 +90,18 @@ afterEvaluate { project ->
     task androidJavadoc(type: Javadoc) {
         source = android.sourceSets.main.java.srcDirs
         classpath += files(android.bootClasspath)
-        classpath += files(project.getConfigurations().getByName('compile').asList())
+        classpath += project.files(android.getBootClasspath().join(File.pathSeparator))
+        classpath += project.files(configurations.implementation)
         include '**/*.java'
     }
  
     task androidJavadocJar(type: Jar, dependsOn: androidJavadoc) {
-        classifier = 'javadoc'
+        archiveClassifier = 'javadoc'
         from androidJavadoc.destinationDir
     }
  
     task androidSourcesJar(type: Jar) {
-        classifier = 'sources'
+        archiveClassifier = 'sources'
         from android.sourceSets.main.java.srcDirs
         include '**/*.java'
     }
@@ -140,12 +120,47 @@ afterEvaluate { project ->
         archives androidJavadocJar
     }
  
-    task installArchives(type: Upload) {
-        configuration = configurations.archives
-        repositories.mavenDeployer {
-            // Deploy to react-native-event-bridge/maven, ready to publish to npm
-            repository url: "file://${projectDir}/../android/maven"
-            configureReactNativePom pom
+    publishing {
+        publications {
+            release(MavenPublication) {
+                from components.findByName("release") ?: components["default"]
+               
+                // Use the same group ID, artifact ID and version from package.json
+                def packageJson = new groovy.json.JsonSlurper().parseText(file('../package.json').text)
+                groupId = "com.futurepress.staticserver"
+                artifactId = packageJson.name
+                version = packageJson.version
+               
+                // Include the artifacts
+                artifact androidSourcesJar
+                artifact androidJavadocJar
+               
+                pom {
+                    name = packageJson.title
+                    description = packageJson.description
+                    url = packageJson.repository?.url ?: ""
+                   
+                    licenses {
+                        license {
+                            name = packageJson.license
+                            url = packageJson.repository?.url ? packageJson.repository.url + '/blob/master/' + (packageJson.licenseFilename ?: 'LICENSE') : ""
+                            distribution = 'repo'
+                        }
+                    }
+                   
+                    developers {
+                        developer {
+                            id = packageJson.author?.username ?: ""
+                            name = packageJson.author?.name ?: ""
+                        }
+                    }
+                }
+            }
+        }
+        repositories {
+            maven {
+                url = uri("${projectDir}/../android/maven")
+            }
         }
     }
 }
\ No newline at end of file
