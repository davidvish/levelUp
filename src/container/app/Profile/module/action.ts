import {createAction} from '@reduxjs/toolkit';

// profile name update ///
export const profileNameRequestAction : any = createAction(
  'request/profileNameRequestAction',
);

export const profilePasswordRequestAction : any = createAction(
    'request/profilePasswordRequestAction',
  );